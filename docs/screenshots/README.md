# Submission screenshot

Save the final challenge screenshot here as:

```text
docs/screenshots/thermalmesh-lab.png
```

Recommended capture: a full-page image at 1440 px wide or larger, after loading the demo scenario and running **Compare routing policies**. Include the recommendation, comparison table, worker-load view, and Agent Activity.
